- Here, we'll use the "Metasploitable" machine server, where there are a lot of web interfaces in which we can train...
- We'll use "DVWA", in which you can choose the security level between (Low, Medium & High), so you can train yourself in hacking that website with different difficulty levels...
- And of course, we'll see multiple types of attacks such as (SQLInjection, XSS...)